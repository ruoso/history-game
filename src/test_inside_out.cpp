#include <iostream>
#include <string>
#include <vector>
#include <io/managed_entity.h>

int main() {
  std::cout << "Testing inside-out-objects library..." << std::endl;
  
  // Simple test to make sure we can compile against the library
  std::cout << "Successfully included managed_entity header" << std::endl;
  
  return 0;
}